<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saiful.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Sop"></activity>
        <activity android:name=".gado" />
        <activity android:name=".otak" />
        <activity android:name=".papeda" />
        <activity android:name=".pempek" />
        <activity android:name=".gudeg" />
        <activity android:name=".Ayam" />
        <activity android:name=".lumpia" />
        <activity android:name=".Keraktelor" />
        <activity android:name=".rendangActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>